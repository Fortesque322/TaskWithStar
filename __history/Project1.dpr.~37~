program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

var
  x,i : integer;
  a : array of string;
  z : real;
  Server: TServer;
  Count : TCount;
  Engineer : TServiceEngineer;

  begin
  try
    Count := TCount.Create();
      readln(z);
      Count.GetSalary(z);
//    readln(x);
//    readln(a[i]);
//    Count.GetEngineerName(x, a);
    Writeln('NO: ' + floattostr(Engineer.FSalary));
    sleep(2000);
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
