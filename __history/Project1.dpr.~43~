program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

var
  x,i : integer;
  a : array of string;
  z : real;
  y : double;
//  Server: TServer;
  Count : TCount;
  //Engineer : TServiceEngineer;

  begin
  try
    Count := TCount.Create();
      Writeln('ЗП: ');
      readln(z);
      Count.GetSalary(z);
      Writeln('Стаж: ');
      Readln(z);
      Count.GetExperience(z);
      Writeln('Счет: ');
      readln(y);
      Count.GetBill(y);

      //readln(x);
//    readln(a[i]);
//    Count.GetEngineerName(x, a);
    Writeln('ЗП: ' + floattostr(Count.ServiceEngineer.FSalary));
    Writeln('Стаж: ' + floattostr(Count.ServiceEngineer.FExperience));
    Writeln('Стаж: ' + floattostr(Count.ServiceCentr.FBill));


    sleep(2000);
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
